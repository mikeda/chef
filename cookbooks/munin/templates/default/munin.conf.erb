includedir /etc/munin/conf.d
graph_strategy cgi

[network;]

[network;vyatta01]
  address 127.0.0.1
  use_node_name no

[server;]

<% node[:munin][:server][:servers].each do |sv| %>
[server;<%= sv[:hostname] %>]
    address <%= sv[:address] %>
    use_node_name yes

<% end %>
